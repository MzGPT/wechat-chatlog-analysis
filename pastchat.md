# 项目对话与改动总览（Past Chat Digest）

更新时间：2025-10-30 00:00:00

## 本轮更新（概要）
- 顶部工具栏更紧凑：时间窗口 + 分析模块 + 运行/导出按钮 + 状态，同行展示；字号与间距整体缩小（12px，紧凑间距）。
- 会议路演表彻底收紧：3 列（时间｜平台/会议号｜主题要点），行高约 20px；列宽（时间 65px、平台 95px、主题单行省略）。主题要点严格取自摘要 summary（去除 `ai:` 前缀），禁止复制原文。
- 新闻“来源气泡”稳定：徽标解析支持 `#123`（消息）与 `#xxx`（新闻）；新增后端 `GET /api/newsfeed/by-ids`；悬停/点击气泡展示来源标题/时间/链接。
- 联系人管理强化：
  - 微信消息行内“顶/踩”复选框直连后端评分接口（±1 分），1 秒后自动复位；
  - 评分单元格可手动输入并保存；
  - 评分降至 40 分以下自动加入黑名单（持久化到 SyncState），并即时在列表隐藏其消息。
- 黑白名单全链条：
  - 设置页展示白/黑名单（联系人/群聊）并可移除；
  - 新增“加载候选”按钮，从 `/api/contacts` 与 `/api/chats` 填充下拉；
  - 黑名单的联系人/群聊/发件人，永远不显示在“微信消息/邮件消息”列表（与筛选无关，强制隐藏）。
- 拉取后重置微信筛选为默认（避免旧筛选掩盖新数据）。
- 邮件不再自动同步或首开跳转，仅手动“同步所选”后刷新列表。
- 首页开启 GZip 压缩，首开更快。
- AI 总结：前端传入的模块提示词优先生效；统一瘦身 LLM payload；新增“进程内+DB 持久化”的增量摘要缓存。

---

## 关键改动（按模块）

### AI 总结（UI/交互）
- 顶部工具栏一行化与紧凑化（时间标签、分析模块、运行/导出、状态齐列）。
- 高级选项常驻展示（取消折叠按钮），收紧字体与内边距。
- 会议路演：仅用 `summary` 生成“主题要点”；3 列定宽（时间 65px、平台 95px、主题自适应单行省略），表头非 sticky、防错位。

文件：
- `static/index.html`（顶部工具栏样式与结构、会议表渲染与样式）

### 新闻舆情监测
- 徽标解析扩展：在新闻卡片内，`#数字` 也当作新闻 ID；其他模块纯数字视作消息 ID、非数字视作新闻 ID。
- 新增 `GET /api/newsfeed/by-ids?ids=...`；前端气泡悬停/点击展示新闻标题/来源/时间/原文链接。

文件：
- `static/index.html`（enhanceSummaryElement、showNewsPopover）
- `app/routers/news.py`（`/api/newsfeed/by-ids`）

### 微信/联系人管理
- 勾选“顶/踩”→ 直接调用 `/api/contacts/{id}/rating?delta=±1`。
- 评分输入 + 保存：评分单元格支持输入 0–100 并保存（计算 delta 后复用后端接口）。
- 自动拉黑：评分 < 40 自动写入黑名单（SyncState.blacklist_senders），并立即隐藏该联系人的消息。
- 黑白名单展示：设置页可查看/移除白/黑名单（联系人/群聊 talkers）；“加载候选”填充下拉（来自 `/api/contacts` 与 `/api/chats`）。
- 列表联动：黑名单 `senders`/`talkers` 永远从“微信消息”中过滤（不依赖筛选控件）。

文件：
- `static/index.html`（消息行 ops、评分保存、自动拉黑、名单渲染与增删、候选加载、applyFilters 黑名单强过滤）
- `app/routers/chats.py`（`/api/chats` 候选）

### 邮件
- 仅在“同步所选”后刷新，不再自动同步/自动跳转到“邮件消息”。
- 黑名单发件人（邮箱地址）一律不显示在邮件列表。

文件：
- `static/index.html`（移除首开自动切至邮件页逻辑；renderEmailTable 增加 `data-email-from`；applyEmailFilters 黑名单过滤）

### 后端与缓存
- 新增摘要增量缓存：
  - 进程内字典缓存 + DB 持久化（SyncState），键：`summary_cache:<snapshot_id>:<module>:<prompt_hash>:<temp>`；命中直接返回。
- 统一瘦身 LLM payload（只传 `messages` 精简字段 + `contact_ratings` + `meta.total_messages`）。
- 数据集落盘改为“瘦身版本”（去掉邮箱地址、附件、URL 等无关大字段）。
- 首页启用 GZip 压缩（`GZipMiddleware`）。

文件：
- `app/routers/ai.py`（缓存读写、payload 瘦身、数据集瘦身、前端 prompts 覆盖）
- `app/main.py`（GZipMiddleWare）

---

## 用量/性能
- Token 控制：模块化筛选消息 + 统一瘦身 payload；新闻上下文仅传精简 `messages`；支持分片合并（newswatch 超长时）。
- 缓存：同一快照+提示词+温度可命中“进程内/持久化”缓存，显著减少调用。

---

## 行为策略
- 黑名单策略：
  - 联系人（senders）：隐藏所有该联系人的微信消息；若为邮箱地址，隐藏其邮件。
  - 群聊（talkers/@chatroom）：隐藏该群所有消息。
  - 设置页可视化并可移除；“加载候选”可填充联系人/群聊 ID。
- 会议主题生成：只允许来自摘要（小模型），避免重复与冗余。
- 拉取后自动重置微信筛选为默认（避免旧筛选导致“看不到新内容”）。
- 邮件仅手动同步，避免“无端切页/自动刷新”。

---

## 快速验证
- 顶部工具栏：时间标签/模块/按钮/状态是否同行显示；缩小字号与间距。
- 会议路演：是否 3 列（时间65px｜平台95px｜主题），主题仅来自摘要，单行省略。
- 新闻气泡：在 AI 总结中悬停/点击 `#id` 是否弹出消息/新闻来源详情；`/api/newsfeed/by-ids` 是否可返回。
- 联系人打分：
  - 微信消息行勾选“顶/踩”是否加/减分；评分<40 是否自动加入黑名单并即时隐藏消息；
  - 联系人管理表评分输入“保存”是否生效。
- 黑白名单面板：是否能显示/移除黑白名单；“加载候选”是否填充下拉。
- 邮件：黑名单发件人是否不显示；是否只在“同步所选”后刷新；首开是否不再跳页。
- 首页：加载是否更快（压缩生效）。

---

## 备注/后续建议
- 可增加“清空摘要缓存”按钮（设置页），一键清理 SyncState 中以 `summary_cache:` 开头的键。
- 可在设置里暴露“自动拉黑阈值”（默认 40 分），或允许关闭自动拉黑。
- 会议主题列如需更短/更短列宽（例如 8 字），可再调节 CSS 与裁剪长度。

---

## 2025-11 中旬至今对话与改动总览

> 本段为 2025-11-10 起，本助手与用户围绕“AI 总结页面重构 + 黑客主题 + 纪要与新闻聚合”等需求的梳理总结。

### 一、AI 总结链路重构

1. 提示词统一与来源收敛
- 所有 AI 总结模块（市场观点 / 会议路演 / 分歧观点 / 高评分联系人 / 新闻舆情）统一从后端配置 `data/ai_config.json` 读取 `module_prompts`，前端只作为编辑界面，不再直接把自定义提示词传给大模型。
- 功能设置页增加“提示词管理”面板：可在 UI 中为各模块/小模型编辑系统提示词与用户模板，并通过 `/api/ai/config` 写回 `ai_config.json`。
- 为兼容旧键，前端在 `getSummaryPrompts()` 中自动映射 legacy key（如 `market_opinions`）到新 key（如 `market`）。

2. 上下文与 Token 策略
- 总结上下文统一改为只使用消息的摘要列 `derived.summary`（微信 + 邮件），不再把长正文 `content` 直接喂给大模型，显著减小上下文体积。
- 对于新闻舆情模块，仍然使用新闻聚合数据（标题/链接/时间/来源），不走聊天消息。
- 在 `app/routers/ai.py` 中，`enriched_messages` 只保留 `summary`、必要元数据与少量关键词，`_compact()` 使用摘要作为唯一上下文；正文仅在前端本地渲染展示。

3. 缓存机制升级
- 原先简单 dict 缓存 `SUMMARY_CACHE` 被升级为进程内 LRU（`OrderedDict`）：
  - key: `(snapshot_id, module_key, prompt_hash, temperature)`；
  - 上限通过环境变量 `SUMMARY_CACHE_MAX` 控制（默认 64）。
- 持久化缓存：继续使用 `SyncState` 表以 `summary_cache:<snap_id>:<module>:<hash>:<temp>` 形式存储。
- 新增接口：
  - `GET /api/ai/debug/caches`：返回内存缓存数量、DB 中 summary_cache 数量，以及新闻缓存条目数；
  - `POST /api/ai/summary/cache/clear`：清空进程内 LRU 与 DB 中所有 `summary_cache:` 记录。

4. AI 总结 UI 与时间展示
- 每个总结模块的标题后增加“生成时间 + 时间窗口”徽标，例如：
  - `分歧观点分析（11-20 10:15，最近1天）`
- 该时间信息在重新加载页面时也会随本地缓存一起恢复：`localStorage.aiSummaryCache.v1` 中保存 `ts` 与 `period`，`restoreAiSummaryFromCache()` 会重新绘制 `small.gen-time`。

5. 模块级增强
- 分歧观点：
  - 后端 `_build_counter_html()` 始终生成 H2 议题标题 + 三列表格（正方 / 反方 / 冲突点），每个议题包含至少一行汇总；
  - 前端为 `.counter-table` 固定宽度比 28/28/44，避免横向滚动。
- 高评分联系人：
  - 严格从联系人管理中的评分抽取：`rating >= 60` 且统计期间内有消息出现；
  - 利用 `name_to_cid` 统一 name/alias/display_name 到 contact_id 后匹配消息；杜绝“联系人库不存在的名字”出现在高评分列表里；
  - HTML 版本 `_build_contacts_html()` 输出 `<li><strong>姓名</strong>（评分 xx.x） + 源徽标 + 最新摘要`。
- 会议路演：
  - 统一从消息的 `summary` 字段生成“主题要点”，禁止复制原文；
  - 时间字段优先解析正文中的会议时间（如 “9-24 19:30”、“9月24日 19:30”），仅在识别失败时回退到消息发送时间，渲染时使用简洁格式 `MM-DD HH:MM`；
  - 平台采用简化名：`腾讯会议→腾讯`、`进门财经→进门`、`电话会议→电话` 等；会议卡片中展示“时间 | 平台/会议号 | 主题要点”，并在 HTML 表格中用 `title` 属性保留完整时间与平台+会议号。

### 二、前端 UI 大重构与黑客主题

1. 消息列表 UI 复原 + 强化
- 在多轮尝试导致 UI 错位/叠加后，回滚至 11 月初较稳定版本，再在其基础上重建：
  - 微信/邮件列表始终为单层表格：`发送人 | 时间 | 聊天对象/账户 | 类型 | 分类 | 情绪 | 摘要 | 原文 | 操作`；
  - 仅前 3 列（发送人/时间/聊天对象）左侧冻结，操作列右侧冻结；中间列正常随滚动。
- 分类（content-type）与情绪（tone）列：
  - 使用简洁贴文字的背景色标记（无额外镶套元素），避免多层 div 造成错位；
  - 统一宽度 40px，文本居中。
- 摘要列：
  - 通过 `.summary-col` + `.summary-text` 组合控制省略/展开，不再改变 `td` display，避免列宽错乱；
  - 不同来源以 class 区分：`ai`（橘黄色）、`fallback`（灰色）。

2. 键盘与鼠标交互
- 为微信消息、邮件消息、纪要聚合三张表增加“单元格选择”能力：
  - 点击任意 `td` 即选中，添加 `.cell-selected` 边框；
  - 方向键上下左右移动光标，支持 `Shift+方向键` 扩展框选区域（`.cell-range` 高亮）；
  - `Enter/Space` 对当前单元格弹出详情气泡（显示原文或完整内容）；`Esc` 关闭气泡。

3. 黑客主题 / 深色主题与浅色主题一致性
- 深色主题（黑客模式）：
  - 全局黑底 + 荧光绿边框/高亮；AI 总结卡、表头、筛选器、弹窗统一暗色背景与浅色文字；
  - 操作按钮、问答/顶踩按钮适配深色主题，不再保留浅色背景导致对比度不足。
- 浅色主题：
  - 顶部 Header 右侧统计文字改为深色，避免白字白底；
  - 主题切换按钮文案为 “🟢 黑客主题 / 🌤 浅色主题”，状态同步 `localStorage.theme`。

4. AI 总结导出长图
- 各总结模块卡片（市场/会议/分歧/联系人/新闻）统一增加“🖼 导出”按钮：
  - 使用 `html2canvas` 将“模块标题 + 内容”渲染为长图；
  - 生成前将卡片克隆为一个 414px 宽的移动端风格容器，关闭 sticky/截断，让内容完整换行；
  - 图片文件名携带模块名与时间戳。

5. 纪要聚合模块
- 新增“纪要聚合”标签页，与微信/邮件结构对齐：
  - 后端 `app/routers/minutes.py` 扫描 `data/minutes`、`data/recordings` 目录中的纪要文本和录音文件；
  - 对每个文件生成虚拟消息：`主讲人（sender_name）/时间/类型/分类=会议/情绪`；
  - 使用小模型工具提示词 `minutes_summary` 生成 ≤300 字的会议摘要，若无 Key 则使用本地规则 `ai: + 前几段压缩`。
- 前端 `minutesTable`：
  - 列：`主讲人｜时间｜来源(目录名)｜类型｜分类｜情绪｜摘要｜原文｜操作`；
  - 搜索框支持按主讲人/正文/文件名过滤；
  - 复用键盘/鼠标选中与气泡逻辑。

### 三、新闻源与舆情分析优化

1. 新闻聚合源扩展
- 在 `app/services/news_client.py` 中，`direct_from_sources_json()` 增加多类源：
  - 海外：WallstreetCN 快讯、Hacker News、Spaceflight News、多个技术/加密媒体、NPR Business；
  - 新增：Reuters Business（RSS）、BBC Business（RSS）、CNBC Top News（RSS）；
  - 国内新增尝试：证券时报、21 世纪经济报道、界面新闻、财新、上证报、澎湃财经、第一财经等 RSS 源。
- 对所有源进行去重（按 id 或 url+title），并可按关键字过滤。

2. 新闻缓存与白名单
- 为直接聚合增加 3 小时内存缓存（`_CACHE`），键为 `direct:limit:q`，减少高频重复请求。
- 白名单 `_load_source_whitelist()`：
  - 若 `data/entities.json` 提供 `news_sources_whitelist`，则使用文件配置；
  - 否则使用默认白名单，涵盖上述主流财经媒体（国外 + 国内）。

3. 前端新闻聚合 UI
- “新闻聚合”页顶部增加内置聚合统计条：
  - 文案：仅展示近 72 小时新闻（优先近 24 小时）；
  - 实时统计：来源数量、近 72 小时新闻条数，以及前 N 个来源的 chip（`名称(条数)`）；
  - “刷新内置聚合”按钮调用 `/api/newsfeed/items?limit=120&whitelist_only=false` 重新统计。
- 仍保留 NewsNow iframe 用于用户自订新闻页面，在无法访问 NewsNow 时显示友好占位说明。

4. 新闻舆情提示词加强
- `newswatch` 模块提示词更新：
  - 强调仅整合近 72 小时（优先近 24 小时）；
  - 要求合并相近主题，去重、避免罗列标题与链接；
  - 输出结构为“总体基调 / 关键风险 / 主题脉络 / 关注动作”，重点写清影响路径与可操作动作。

### 四、内存占用与长时间运行

1. 当前进程内存概况（示例）
- 主后端：`uvicorn app.main:app --host 127.0.0.1 --port 8000`，RSS ~150MB 级；
- 辅助 dev 实例（如果存在）：`uvicorn app.main:app --host 0.0.0.0 --port 5167 --reload`，~40–60MB；
- 整体在 200MB 以内，对本机来说属于轻量服务。

2. 可能增长点与控制手段
- 增长点：
  - AI 总结 LRU 缓存（可由 `SUMMARY_CACHE_MAX` 控制）；
  - 新闻 `_CACHE` 聚合缓存；
  - 浏览器端加载的 index.html + 大量 DOM（前端进程内存）。
- 控制措施：
  - 利用 `POST /api/ai/summary/cache/clear` 周期性清空；
  - 适当减小 `SUMMARY_CACHE_MAX`（如从 64 降到 32）；
  - 新闻聚合已有限制条数与 TTL=3h 的缓存，不会无限增长。

### 五、仍可继续优化的方向（待办）

- 为 AI 总结各模块增加 UI 级“清空总结缓存”和“查看缓存统计”按钮（调用新接口）。
- 为纪要聚合增加本地/离线转录（录音 → 文本），统一走 `minutes_summary` 管道。
- 新闻聚合：
  - 进一步校验国内 RSS 源连通性与解析稳定性，对异常源做回退；
  - 根据用户偏好增加“只看 A 股宏观/行业/个股”分组视图。
- 深色/浅色主题继续收尾：确保所有复合组件（弹窗、图表、tooltip）在两种主题下文字与背景对比度足够。

